
import Foundation

import UIKit

class PAItemAymentCell: UICollectionViewCell {
var contextReplaceTimelabelDictionary: [String: Any]!
private var photoAll_list: [Any]!
private var recordsSize: Double = 0.0




    @IBOutlet weak var sepakImage: UIImageView!

@discardableResult
 func settingChildUnsafeTemporaryLibraryBriefButton(progressYhlogo: String!) -> UIButton! {
    var detectionY: Double = 0.0
   withUnsafeMutablePointer(to: &detectionY) { pointer in
          _ = pointer.pointee
   }
    var main_wc: String! = String(cString: [115,101,112,97,114,97,116,101,0], encoding: .utf8)!
    _ = main_wc
      main_wc.append("\(Int(detectionY))")
   for _ in 0 ..< 1 {
      detectionY -= Double(1)
   }
   if 1.84 >= (2.4 - detectionY) && (1 << (Swift.min(4, main_wc.count))) >= 4 {
      detectionY /= Swift.max(4, (Double(main_wc == (String(cString:[74,0], encoding: .utf8)!) ? Int(detectionY) : main_wc.count)))
   }
   for _ in 0 ..< 1 {
      detectionY -= Double(3 & main_wc.count)
   }
     let sumDisconnect: [String: Any]! = [String(cString: [105,112,97,100,0], encoding: .utf8)!:1970.0]
     var aimageGundong: UIView! = UIView()
     let msgMessages: UIButton! = UIButton(frame:CGRect.zero)
     var iconDetect: UIView! = UIView(frame:CGRect(x: 206, y: 115, width: 0, height: 0))
    var strncasecmpSchiAnswers = UIButton()
    strncasecmpSchiAnswers.titleLabel?.font = UIFont.systemFont(ofSize:13)
    strncasecmpSchiAnswers.setImage(UIImage(named:String(cString: [97,110,115,119,101,114,0], encoding: .utf8)!), for: .normal)
    strncasecmpSchiAnswers.setTitle("", for: .normal)
    strncasecmpSchiAnswers.setBackgroundImage(UIImage(named:String(cString: [99,111,110,118,101,114,116,0], encoding: .utf8)!), for: .normal)
    strncasecmpSchiAnswers.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    strncasecmpSchiAnswers.alpha = 0.2
    strncasecmpSchiAnswers.frame = CGRect(x: 288, y: 57, width: 0, height: 0)
    aimageGundong.alpha = 0.5;
    aimageGundong.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    aimageGundong.frame = CGRect(x: 5, y: 2, width: 0, height: 0)
    
    var aimageGundongFrame = aimageGundong.frame
    aimageGundongFrame.size = CGSize(width: 228, height: 248)
    aimageGundong.frame = aimageGundongFrame
    if aimageGundong.isHidden {
         aimageGundong.isHidden = false
    }
    if aimageGundong.alpha > 0.0 {
         aimageGundong.alpha = 0.0
    }
    if !aimageGundong.isUserInteractionEnabled {
         aimageGundong.isUserInteractionEnabled = true
    }

    msgMessages.frame = CGRect(x: 78, y: 256, width: 0, height: 0)
    msgMessages.alpha = 0.4;
    msgMessages.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    msgMessages.setBackgroundImage(UIImage(named:String(cString: [115,99,114,111,108,108,0], encoding: .utf8)!), for: .normal)
    msgMessages.titleLabel?.font = UIFont.systemFont(ofSize:15)
    msgMessages.setImage(UIImage(named:String(cString: [113,117,101,115,116,105,111,110,0], encoding: .utf8)!), for: .normal)
    msgMessages.setTitle("", for: .normal)
    
    var msgMessagesFrame = msgMessages.frame
    msgMessagesFrame.size = CGSize(width: 254, height: 129)
    msgMessages.frame = msgMessagesFrame
    if msgMessages.isHidden {
         msgMessages.isHidden = false
    }
    if msgMessages.alpha > 0.0 {
         msgMessages.alpha = 0.0
    }
    if !msgMessages.isUserInteractionEnabled {
         msgMessages.isUserInteractionEnabled = true
    }

    iconDetect.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    iconDetect.alpha = 0.7
    iconDetect.frame = CGRect(x: 242, y: 101, width: 0, height: 0)
    
    var iconDetectFrame = iconDetect.frame
    iconDetectFrame.size = CGSize(width: 268, height: 289)
    iconDetect.frame = iconDetectFrame
    if iconDetect.isHidden {
         iconDetect.isHidden = false
    }
    if iconDetect.alpha > 0.0 {
         iconDetect.alpha = 0.0
    }
    if !iconDetect.isUserInteractionEnabled {
         iconDetect.isUserInteractionEnabled = true
    }


    
    var strncasecmpSchiAnswersFrame = strncasecmpSchiAnswers.frame
    strncasecmpSchiAnswersFrame.size = CGSize(width: 299, height: 203)
    strncasecmpSchiAnswers.frame = strncasecmpSchiAnswersFrame
    if strncasecmpSchiAnswers.isHidden {
         strncasecmpSchiAnswers.isHidden = false
    }
    if strncasecmpSchiAnswers.alpha > 0.0 {
         strncasecmpSchiAnswers.alpha = 0.0
    }
    if !strncasecmpSchiAnswers.isUserInteractionEnabled {
         strncasecmpSchiAnswers.isUserInteractionEnabled = true
    }

    return strncasecmpSchiAnswers

}





    override func awakeFromNib() {

         let intersperseAttributes: UIButton! = settingChildUnsafeTemporaryLibraryBriefButton(progressYhlogo:String(cString: [112,105,101,0], encoding: .utf8)!)

      if intersperseAttributes != nil {
          let intersperseAttributes_tag = intersperseAttributes.tag
          self.addSubview(intersperseAttributes)
      }

_ = intersperseAttributes


       var statuesB: Double = 3.0
    var againH: String! = String(cString: [116,119,111,108,111,111,112,0], encoding: .utf8)!
      againH.append("\(againH.count % (Swift.max(3, 4)))")

      againH.append("\(againH.count % (Swift.max(1, 5)))")
        super.awakeFromNib()
   if statuesB < Double(againH.count) {
      statuesB += Double(Int(statuesB))
   }
        
        sepakImage.layer.borderWidth = 3
      againH = "\(Int(statuesB) % (Swift.max(againH.count, 4)))"
        sepakImage.layer.cornerRadius = 16
        sepakImage.layer.masksToBounds = true
    }

}
