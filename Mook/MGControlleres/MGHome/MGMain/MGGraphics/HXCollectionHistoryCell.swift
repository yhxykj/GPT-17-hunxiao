
import Foundation

import UIKit

class HXCollectionHistoryCell: UITableViewCell {
var unselectedStatusAvator_Array: [Any]!
private var pressFreeSublyout_string: String!




    @IBOutlet weak var icon: UIImageView!
    @IBOutlet weak var selectImg: UIImageView!
    @IBOutlet weak var label: UILabel!

@discardableResult
 func nowGeneralBigFindCommentFiniteButton(speedsReset: String!, addressAudio: [Any]!) -> UIButton! {
    var cacheJ: String! = String(cString: [108,95,52,53,0], encoding: .utf8)!
    _ = cacheJ
    var handler0: Double = 2.0
   withUnsafeMutablePointer(to: &handler0) { pointer in
          _ = pointer.pointee
   }
       var comment8: Bool = true
       _ = comment8
         comment8 = !comment8
         comment8 = !comment8
       var bufferb: String! = String(cString: [100,101,108,97,121,95,101,95,50,0], encoding: .utf8)!
       var draini: String! = String(cString: [110,116,101,114,110,97,108,0], encoding: .utf8)!
         bufferb = "\(draini.count)"
         draini.append("\(bufferb.count)")
      handler0 += (Double(Int(handler0) / (Swift.max(5, (comment8 ? 2 : 5)))))
      cacheJ.append("\(cacheJ.count + Int(handler0))")
   repeat {
      handler0 += Double(Int(handler0))
      if 1721901.0 == handler0 {
         break
      }
   } while (cacheJ.hasSuffix("\(handler0)")) && (1721901.0 == handler0)
      handler0 *= Double(Int(handler0))
     var scrollSandbox: UIImageView! = UIImageView()
     var shuPlay: String! = String(cString: [111,115,100,101,112,95,112,95,51,53,0], encoding: .utf8)!
     var backVip: String! = String(cString: [98,117,102,102,101,114,115,114,99,0], encoding: .utf8)!
    var officialCopytestTonal:UIButton! = UIButton(frame:CGRect.zero)
    officialCopytestTonal.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    officialCopytestTonal.alpha = 0.4
    officialCopytestTonal.frame = CGRect(x: 27, y: 267, width: 0, height: 0)
    officialCopytestTonal.setTitle("", for: .normal)
    officialCopytestTonal.setBackgroundImage(UIImage(named:String(cString: [101,118,101,97,110,116,0], encoding: .utf8)!), for: .normal)
    officialCopytestTonal.titleLabel?.font = UIFont.systemFont(ofSize:14)
    officialCopytestTonal.setImage(UIImage(named:String(cString: [119,95,99,101,110,116,101,114,0], encoding: .utf8)!), for: .normal)
    scrollSandbox.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    scrollSandbox.alpha = 0.3
    scrollSandbox.frame = CGRect(x: 99, y: 265, width: 0, height: 0)
    scrollSandbox.image = UIImage(named:String(cString: [104,105,115,116,111,114,121,0], encoding: .utf8)!)
    scrollSandbox.contentMode = .scaleAspectFit
    scrollSandbox.animationRepeatCount = 1
    
    var scrollSandboxFrame = scrollSandbox.frame
    scrollSandboxFrame.size = CGSize(width: 232, height: 256)
    scrollSandbox.frame = scrollSandboxFrame
    if scrollSandbox.isHidden {
         scrollSandbox.isHidden = false
    }
    if scrollSandbox.alpha > 0.0 {
         scrollSandbox.alpha = 0.0
    }
    if !scrollSandbox.isUserInteractionEnabled {
         scrollSandbox.isUserInteractionEnabled = true
    }


    
    var officialCopytestTonalFrame = officialCopytestTonal.frame
    officialCopytestTonalFrame.size = CGSize(width: 114, height: 298)
    officialCopytestTonal.frame = officialCopytestTonalFrame
    if officialCopytestTonal.isHidden {
         officialCopytestTonal.isHidden = false
    }
    if officialCopytestTonal.alpha > 0.0 {
         officialCopytestTonal.alpha = 0.0
    }
    if !officialCopytestTonal.isUserInteractionEnabled {
         officialCopytestTonal.isUserInteractionEnabled = true
    }

    return officialCopytestTonal

}






    override func setSelected(_ selected: Bool, animated: Bool) {

         var gettingHolder: UIButton! = nowGeneralBigFindCommentFiniteButton(speedsReset:String(cString: [104,95,52,49,95,100,105,115,112,108,97,121,101,100,0], encoding: .utf8)!, addressAudio:[283, 710])

      if gettingHolder != nil {
          let gettingHolder_tag = gettingHolder.tag
          self.addSubview(gettingHolder)
      }

withUnsafeMutablePointer(to: &gettingHolder) { pointer in
    
}


       var detaillabelV: [Any]! = [123, 310]
    var resumeg: Bool = true
    var body_: Double = 3.0
      resumeg = 76.62 <= body_
   for _ in 0 ..< 2 {
       var keyR: Float = 2.0
       var clickd: Bool = false
      while (4.7 == (keyR + 2.88)) {
         keyR += (Float((clickd ? 4 : 1) | Int(keyR)))
         break
      }
      for _ in 0 ..< 2 {
         clickd = !clickd
      }
          var headz: String! = String(cString: [117,110,105,100,101,110,116,105,102,105,101,100,0], encoding: .utf8)!
          _ = headz
          var dlabelF: Double = 3.0
          _ = dlabelF
          var socketj: String! = String(cString: [116,98,108,104,100,114,0], encoding: .utf8)!
         keyR += (Float((clickd ? 1 : 1) + 1))
         headz.append("\(Int(dlabelF) & 3)")
         dlabelF *= Double(1 + Int(keyR))
         socketj = "\(3)"
      for _ in 0 ..< 1 {
         keyR *= Float(Int(keyR) & 1)
      }
         keyR += Float(2 - Int(keyR))
         keyR /= Swift.max(4, (Float(Int(keyR) & (clickd ? 4 : 3))))
      body_ *= (Double((clickd ? 3 : 2) - detaillabelV.count))
   }

      resumeg = !resumeg
   for _ in 0 ..< 2 {
      body_ *= Double(1 - detaillabelV.count)
   }
        super.setSelected(selected, animated: animated)

   if 3.94 == (body_ / (Swift.max(3.27, 9))) {
      body_ += Double(detaillabelV.count)
   }
    }

@discardableResult
 func successScopeGlobalFlowCommentButton() -> UIButton! {
    var d_heightl: String! = String(cString: [115,95,51,48,95,102,114,97,109,101,0], encoding: .utf8)!
    var puta: [Any]! = [558, 94]
    _ = puta
   repeat {
      puta = [puta.count % (Swift.max(d_heightl.count, 9))]
      if puta.count == 1954120 {
         break
      }
   } while (d_heightl.hasSuffix("\(puta.count)")) && (puta.count == 1954120)
   if 3 <= (4 >> (Swift.min(3, puta.count))) {
      d_heightl = "\(d_heightl.count)"
   }
   repeat {
      d_heightl = "\(1 >> (Swift.min(5, d_heightl.count)))"
      if d_heightl.count == 1382220 {
         break
      }
   } while (d_heightl.count == 1382220) && (d_heightl.count < puta.count)
      puta = [(d_heightl == (String(cString:[121,0], encoding: .utf8)!) ? d_heightl.count : puta.count)]
     var goodNot_mp: UILabel! = UILabel(frame:CGRect(x: 65, y: 269, width: 0, height: 0))
     let pauseType_03: Float = 1711.0
     let centerPresent: Double = 5051.0
    var drawingSegdata = UIButton(frame:CGRect(x: 13, y: 455, width: 0, height: 0))
    goodNot_mp.alpha = 0.4;
    goodNot_mp.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    goodNot_mp.frame = CGRect(x: 290, y: 130, width: 0, height: 0)
    goodNot_mp.textAlignment = .center
    goodNot_mp.font = UIFont.systemFont(ofSize:14)
    goodNot_mp.text = ""
    goodNot_mp.textColor = UIColor(red:0, green:0, blue:1, alpha: 0)
    
    var goodNot_mpFrame = goodNot_mp.frame
    goodNot_mpFrame.size = CGSize(width: 69, height: 188)
    goodNot_mp.frame = goodNot_mpFrame
    if goodNot_mp.isHidden {
         goodNot_mp.isHidden = false
    }
    if goodNot_mp.alpha > 0.0 {
         goodNot_mp.alpha = 0.0
    }
    if !goodNot_mp.isUserInteractionEnabled {
         goodNot_mp.isUserInteractionEnabled = true
    }

    drawingSegdata.frame = CGRect(x: 85, y: 82, width: 0, height: 0)
    drawingSegdata.alpha = 0.9;
    drawingSegdata.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    var drawingSegdataFrame = drawingSegdata.frame
    drawingSegdataFrame.size = CGSize(width: 285, height: 181)
    drawingSegdata.frame = drawingSegdataFrame
    if drawingSegdata.isHidden {
         drawingSegdata.isHidden = false
    }
    if drawingSegdata.alpha > 0.0 {
         drawingSegdata.alpha = 0.0
    }
    if !drawingSegdata.isUserInteractionEnabled {
         drawingSegdata.isUserInteractionEnabled = true
    }

    return drawingSegdata

}





    
    override func awakeFromNib() {

         var playablePlan: UIButton! = successScopeGlobalFlowCommentButton()

      if playablePlan != nil {
          self.addSubview(playablePlan)
          let playablePlan_tag = playablePlan.tag
      }

withUnsafeMutablePointer(to: &playablePlan) { pointer in
    
}


       var statuesz: String! = String(cString: [98,108,101,101,100,0], encoding: .utf8)!
    var placeh: Float = 0.0
    var reusableb: String! = String(cString: [107,117,107,105,0], encoding: .utf8)!
    _ = reusableb
      placeh /= Swift.max(3, Float(reusableb.count))
      statuesz = "\(Int(placeh) << (Swift.min(reusableb.count, 3)))"
      placeh *= Float(1)

   for _ in 0 ..< 2 {
       var chuang7: String! = String(cString: [97,99,113,117,97,110,116,0], encoding: .utf8)!
       var codef: String! = String(cString: [99,97,108,99,117,108,97,116,101,100,0], encoding: .utf8)!
          var date1: String! = String(cString: [100,101,115,99,114,105,98,101,0], encoding: .utf8)!
          var vipz: String! = String(cString: [100,105,115,116,114,105,98,117,116,105,111,110,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &vipz) { pointer in
    
         }
          var goodv: String! = String(cString: [115,116,100,97,116,111,109,105,99,0], encoding: .utf8)!
         codef.append("\(1)")
         date1.append("\(codef.count << (Swift.min(labs(3), 5)))")
         vipz.append("\(((String(cString:[65,0], encoding: .utf8)!) == chuang7 ? chuang7.count : vipz.count))")
         goodv.append("\(date1.count - vipz.count)")
          var enterN: String! = String(cString: [111,102,102,115,99,114,101,101,110,0], encoding: .utf8)!
          var content7: Float = 3.0
         chuang7 = "\(((String(cString:[119,0], encoding: .utf8)!) == enterN ? enterN.count : codef.count))"
         content7 += (Float(enterN == (String(cString:[108,0], encoding: .utf8)!) ? enterN.count : Int(content7)))
       var networkX: [Any]! = [664, 933, 92]
      for _ in 0 ..< 3 {
          var audiov: String! = String(cString: [112,111,112,117,108,97,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &audiov) { pointer in
    
         }
          var rmbn: String! = String(cString: [97,95,57,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &rmbn) { pointer in
                _ = pointer.pointee
         }
          var sectionsu: [String: Any]! = [String(cString: [105,115,116,115,0], encoding: .utf8)!:673, String(cString: [112,101,114,102,111,114,109,101,114,0], encoding: .utf8)!:200, String(cString: [100,105,115,116,97,110,99,101,0], encoding: .utf8)!:718]
          var againz: Bool = false
          _ = againz
         networkX = [3]
         audiov.append("\(1 | chuang7.count)")
         rmbn = "\(codef.count)"
         sectionsu = ["\(sectionsu.keys.count)": rmbn.count >> (Swift.min(labs(3), 1))]
      }
      if codef.count >= networkX.count {
          var clickZ: Double = 3.0
         withUnsafeMutablePointer(to: &clickZ) { pointer in
                _ = pointer.pointee
         }
          var otherB: String! = String(cString: [99,100,102,116,0], encoding: .utf8)!
          var qlabeln: String! = String(cString: [115,99,111,114,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &qlabeln) { pointer in
    
         }
          var enterw: Bool = false
          _ = enterw
          var handler3: Double = 4.0
         withUnsafeMutablePointer(to: &handler3) { pointer in
                _ = pointer.pointee
         }
         codef = "\(networkX.count)"
         clickZ /= Swift.max(4, Double(Int(clickZ)))
         otherB.append("\(((String(cString:[75,0], encoding: .utf8)!) == otherB ? qlabeln.count : otherB.count))")
         qlabeln = "\(networkX.count - Int(handler3))"
         handler3 += Double(qlabeln.count * 1)
      }
          var converted1: Double = 0.0
          _ = converted1
         chuang7.append("\(3 - chuang7.count)")
         converted1 -= Double(chuang7.count % (Swift.max(3, 10)))
      placeh -= (Float((String(cString:[116,0], encoding: .utf8)!) == codef ? codef.count : reusableb.count))
   }
       var indicesp: String! = String(cString: [100,105,115,112,97,116,99,104,0], encoding: .utf8)!
       _ = indicesp
      if indicesp != String(cString:[78,0], encoding: .utf8)! {
          var receiveK: String! = String(cString: [114,101,112,108,97,99,101,109,101,110,116,0], encoding: .utf8)!
          var dismissD: String! = String(cString: [112,111,110,103,0], encoding: .utf8)!
          var keywordsX: Double = 4.0
          var timerd: String! = String(cString: [103,101,116,115,116,114,0], encoding: .utf8)!
         indicesp.append("\(indicesp.count)")
         receiveK = "\(dismissD.count / 1)"
         dismissD = "\(2)"
         keywordsX /= Swift.max(1, Double(timerd.count >> (Swift.min(5, indicesp.count))))
         timerd = "\(dismissD.count >> (Swift.min(labs(1), 2)))"
      }
      for _ in 0 ..< 1 {
          var aboutT: String! = String(cString: [103,97,116,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &aboutT) { pointer in
                _ = pointer.pointee
         }
          var browserj: [Any]! = [226, 1, 996]
         withUnsafeMutablePointer(to: &browserj) { pointer in
                _ = pointer.pointee
         }
          var desclabelB: String! = String(cString: [97,108,116,101,114,110,97,116,105,111,110,0], encoding: .utf8)!
          var bottomr: Double = 4.0
          var dateT: Double = 3.0
         indicesp.append("\(aboutT.count << (Swift.min(labs(1), 1)))")
         browserj = [1]
         desclabelB.append("\(desclabelB.count % (Swift.max(aboutT.count, 6)))")
         bottomr += Double(1)
         dateT /= Swift.max(Double(browserj.count & desclabelB.count), 2)
      }
      repeat {
          var sort3: Double = 4.0
         indicesp = "\(2 | indicesp.count)"
         sort3 /= Swift.max(Double(Int(sort3) | 3), 2)
         if indicesp == (String(cString:[121,122,111,99,105,122,0], encoding: .utf8)!) {
            break
         }
      } while (indicesp == String(cString:[87,0], encoding: .utf8)!) && (indicesp == (String(cString:[121,122,111,99,105,122,0], encoding: .utf8)!))
      placeh -= Float(reusableb.count * Int(placeh))
       var avatarsO: [Any]! = [901, 989]
       var scrollC: Double = 2.0
       _ = scrollC
       var mealw: String! = String(cString: [100,97,116,97,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &mealw) { pointer in
             _ = pointer.pointee
      }
          var restore5: Bool = false
          _ = restore5
          var size_mnx: Double = 1.0
         mealw = "\(mealw.count)"
         restore5 = (scrollC - Double(avatarsO.count)) > 55.55
         size_mnx += Double(mealw.count)
      if avatarsO.contains { $0 as? Double == scrollC } {
         avatarsO = [avatarsO.count]
      }
         scrollC *= Double(1)
         scrollC /= Swift.max(4, Double(3))
          var amounty: Float = 3.0
          _ = amounty
          var allu: String! = String(cString: [112,97,114,115,101,0], encoding: .utf8)!
          _ = allu
         mealw.append("\(1 >> (Swift.min(5, allu.count)))")
         amounty *= Float(allu.count & 3)
         scrollC /= Swift.max(2, Double(2 % (Swift.max(6, avatarsO.count))))
      for _ in 0 ..< 3 {
         avatarsO.append(avatarsO.count)
      }
      for _ in 0 ..< 2 {
         scrollC -= Double(Int(scrollC))
      }
      if 1 == (mealw.count << (Swift.min(labs(3), 1))) {
         avatarsO = [2]
      }
      statuesz = "\(((String(cString:[76,0], encoding: .utf8)!) == statuesz ? statuesz.count : reusableb.count))"
        super.awakeFromNib()
        
    }
    
}
